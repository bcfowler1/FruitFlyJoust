using UnityEngine;

namespace FruitFlyJoust
{
    // Marks generated plant surfaces that can support a fly across adjacent facets.
    public sealed class FlyGripSurface : MonoBehaviour
    {
        public bool allowFacetWrap;
    }
}
