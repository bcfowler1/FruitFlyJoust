# Project requirements

User direction September 18: use the brain model as much as supported. Prioritize the full-connectome/live articulated-body path and tested motor/sensory integration over additional decorative fast-mode pose coding. Unity should display scientific body states in that path. Never label hybrid leg gait, trained head stabilization or externally assisted contact transitions as connectome motor control. Brain-to-leg flight tucking/landing remains unvalidated; do not invent mappings. Preserve the fast approximation as an explicit alternative and the rider mass ratio of one third.

Upstream PFL3 diagnostic now establishes network-dependent steering: connected weights generate DN output; isolated weights yield zero. Three .9 s loaded-body drive-.7 trials (left/right/neutral) passed basic engineering gates; full range/transport/contact requalification pending. Direct DNa01/02 excitation produced identical steering readout under synaptic ablation in tested conditions. Do not claim wider-network causality for that old readout. Latest upstream route now passed nine short loaded-body cases using motor feedback gain .32 and sensory yaw smoothing tau .12, plus live idle/pause/perch/unloading/remount/reset; explicit experimental Unity toggle and Brain/Live Body preset are wired and C# compiled. Unity verification September18 passed 30 gameplay checks (including native animated wing hinge invariance), rider poses (3 orientations/9 transitions), 17 upstream full-brain combat checks (three fixed-target walking phases) and 8 pause/projectile/animation timing checks. One earlier scientific lance timeout remains preserved; phase-dependent reliability is not established. This supersedes offline-only status; synthetic inputs and hybrid gait remain unvalidated biological mappings. Read latest CURRENT_STATUS.md before integration.

Latest integration (supersedes older milestone exclusions below): WalkingLab now supports gameplay combat on the scientific-body clock, actual rider unloading/restoration, hand-attached weapons, 0.18 s sword windup, optional artistic thorax leg IK, and optional practice/enemy encounters. Fourteen scientific combat checks passed body-only and fifteen with all 138639 neurons; nine scientific enemy and eight clock/toggle checks passed. Exact unchanged-time observation caching gave 1.1899× speedup with zero tested state/reset differences; defaults on, full physics/gait retained. Full-brain perch/launch/relanding/resume and rider-load regression passed. A headless same-FlyBody walking→wing-flight probe passed two short hover conditions without pose/velocity reset or root lift forces, but the lower hover condition failed. No playable unified wing landing/contact mode or biologically validated controller is established. See Research/CURRENT_STATUS.md for current evidence and reproduction limits. Do not describe gameplay damage, artistic IK or synthetic descending excitation as validated biology.

Latest walking milestone: physical floor steering is qualified for drive 0.5–0.9 (held fit RMS .087347 rad/s; closed-loop RMS .100485 rad/s, window averages). Outside this range remains raw engineering drive. Neural rate-to-gait decoding and direct descending speed/yaw/contact excitation remain biologically unvalidated. Same NeuroMechFly physics supports walking, claw-only grip, externally assisted launch/approach, contact-only verification and walking resume. Floor/wall/ceiling two-cycle qualification and full-brain UDP pause/reset/continuous-clock checks passed. Unity wall six-foot hold and Space zero-contact launch observed; rider remained attached. Ceiling passed both claw-only landing holds and is enabled. One flat surface with static surface-relative gravity, fixed stance and external wrench; not wing/neural flight or floor-wall-corner traversal. FlyBody wing flight remains a separate backend. Keep this distinction in status and controls.

NeuroMechFly now has a separate loaded WalkingLab, using FlyGym 1.1.0 and optional full FlyWire v783 CUDA DNa01/DNa02 spike steering. The interface directly excites descending cells using synthetic rider/body-speed cues; rate-to-gait steering is uncalibrated, forward drive is engineering and oDN1 is not mapped. Never call this exact Eon reproduction or biological sensory encoding. Full model windows are 15 ms; pause/reset/resume passed for both clocks. Loaded 1.5 s walk moved 14.6 mm and stayed finite. Native mass units are reported without an unverified mg conversion; ratio is one third. Actual Unity brain on/off launch and clean stop passed; .03 real-time observed. User requested larger, lower, forward rider just behind head on thorax ahead of wings: WalkingLab visual scale .78, seat height -.38, forward .28; visually checked. Does not change mass ratio. Flight/perches and combat remain separate backends. See Research/NEUROMECHFLY.md.

September 17 follow-up: continuous surface-relative cruise, landing at current tangential position, real claw hold and launch now share one full-body physics instance. Two cycles passed on floor/wall/ceiling; wall/ceiling UDP movement/hold/launch/reset passed. Unity wall held four claws and Space launched with zero contacts, animated rider attached. Engineering forces/yaw, fixed stance, infinite flat surface, 5 cm excursion bound; not trained-wing or neural landing control. Turning fits failed declared qualification limits and are not enabled. BorrowedRider assets are local copies from D:/Unity Projects/Silverspur; provenance.json records source hashes. Root motion disabled, combat upper-body mask preserves mounted legs. Unity animation sampling verified sword/bow/walk and zero mounted leg difference. Hand/weapon alignment and action timing remain to tune. Source project remains untouched.

Latest calibration: the empirical translation lab fits a stable velocity response to ten loaded full-flight references, with held-out reference 48 RMS error 0.018897 m/s and max 0.041977 m/s. It streams at approximately real time. Do not describe it as a reduced connectome, calibrated yaw/rein control, or collision/landing/combat simulation. The combat prototype is still uncalibrated. PerchTransition provides a separate full-body flat-surface experiment with synthetic averaged flight wrench, settled four-claw contact handover, actual adhesion and launch. Wall/ceiling stream checks passed; wall approach and rider Space launch were observed in Unity. No root welding, but no neural or wing-policy landing control either.

The user approved keeping the full scientific reference alongside a faster condensed model and requested system toggles/sliders. Simulation Controls now switches among the existing fast gameplay approximation, trained physical flight, experimental full-connectome flight, and separate contact labs. Never call the current fast prototype a calibrated reduction; comparisons and reduction work remain required. Live neural influence scales wing residuals while retaining full brain computation; the brain toggle selects a different backend. Grip strength scales actual contact-only forces. Preserve these distinctions.

The central objective is a rider game built around an embodied, connectome-based Drosophila brain/body simulation. This is an explicit user requirement, not an optional future enhancement. The Unity placeholder brain/motor are temporary scaffolding. Do not silently replace the scientific model goal with generic animal AI or appearance-only realism.

The user refers to the fruit-fly brain/body model currently receiving public attention. Eon's FlyWire/Shiu-style brain + NeuroMechFly/MuJoCo work is a likely reference, but the exact implementation/data version has not been explicitly confirmed. Consult SIMULATION_DIRECTION.md and primary sources before selecting the backend. FlyBody is a body model, not itself the requested whole-brain model.

Keep the fly brain, embodied physics, rider inputs, rendering, and combat separately replaceable. Rider cues should enter a defined interface; do not equate raw joystick values with biological motor-neuron activity. Account for sensory feedback from body/world to brain. Never claim connectome integration, biological validation, or play testing that has not occurred.

Keep the rider and reins. While mounted the rider grips with his legs and stays attached at every orientation, including walls/ceilings; dismounting is deliberate. CombatLab implements prototype dismount/remount, on-foot sword/archery, and mounted archery/lance impacts. CombatEncounter adds enemy pursuit/melee/archery, rider health/defeat/reset. These use the gameplay placeholder fly, without combat animations, and do not demonstrate combat on the scientific body.

Confirmed controls: left/right left stick gives rein cues, stick back requests climb, stick forward requests dive, A press gives an initial speed burst settling to a smaller cruising increase, B press lowers speed, holding B requests landing, right stick independently controls look/aim. Retain those gameplay requests as interfaces while evaluating their effect on the scientific controller.

See VALIDATION.md for actual checks and limitations. The existing Unity geometric wall/ceiling attachment is an approximation; it is not a six-leg adhesion simulation.

The user specified rider mass as one third of fly mass and authorized remaining integration. Scientific flight/contact backends now add 0.328207 mg of rigid rider inertia to the 0.984621 mg fly. An opt-in experimental connectome loop uses synthetic feedback-driven DNg02 excitation and actual spike-to-wing residuals over a trained stabilizer. Do not call this biological sensory encoding or a calibrated motor decoder. A separate six-claw MuJoCo adhesion lab has actual contact forces and passed loaded wall/ceiling hold/release checks, but has no approach/landing controller. Combined scientific flight/perching/combat and real-time whole-model performance remain outstanding.

## Final follow-up qualification
The full-connectome unloaded idle stream passed: 138639 neurons, continuous brain/body clocks, 1.044 mm excursion, six landed claws, pause and remount. See walking-idle-stream-evaluation.json. WalkingLab displays unloaded resting/walking state. Fast surface walking now checks the proposed next footprint before stepping.

Two same-FlyBody wing-driven floor-landing probes failed the declared stability limits. Maintaining the flight attitude tipped the body during contact; requesting an upright ghost reference also became unstable. Neither physical pose/time was reset, and no root lift was added. These failed probes are not enabled in Unity. See unified-landing-evaluation.json and unified-upright-landing-evaluation.json. Reproduce with evaluate_unified_policies.py --landing --output unified-upright-landing-evaluation.json; --retain-flight-attitude selects the first approach. Hover-only behavior remains the default.

Research still required: a controller qualified across airborne/contact dynamics, claw-specific landing verification, walking resumption after wing landing, wall/ceiling flight transitions, biological head control, calibrated neural sensory/motor decoding, and real-time whole-model performance. These are unresolved research requirements, not completed gameplay features. Controller feel and exact mounted animation fit still require human playtesting.

## Trained live head stabilization
Floor WalkingLab now offers Trained head stabilization (floor) in Simulation Controls; Apply restarts the body with the selected neck system. This uses FlyGym 1.1's bundled 48-input, 32/32-hidden, two-output proprioceptive/contact MLP and actual neck actuators, not decorative scanning or FlyWire head-neuron decoding. NumPy export preserves weights and preprocessing; 1000-input parity maximum difference was 2.9802e-7 against PyTorch. Apache-2.0 source/license retained in Research/NEUROMECHFLY_LICENSE.txt.

Loaded/unloaded gait, verified six-claw perch and remount passed. Full 138639-neuron idle stream with trained head control passed continuous clocks, 1.1178 mm excursion, six final claws, exact paused geometry and remount. Requalification of the existing physical floor steering controller with head actuation passed six signed-turn trials across drives .5/.7/.9: RMS .09239 rad/s, maximum .17380 rad/s, original limits unchanged. Evidence: head-controller-parity.json, head-walking-evaluation.json, head-idle-stream-evaluation.json, head-walking-controller-evaluation.json. Head control defaults off and is restricted to floor; loaded whole-animal biological validation and full connectome head decoding remain unresolved.

Landing contact counting now distinguishes six individual claw collision geometries from body/wing ground collisions. A live physical test counted six claws upright and zero claws/three non-claw contacts inverted (landing-contact-evaluation.json). New wing landing variants use this stricter gate; first-claw walking handover and resting stance variants still failed. Experimental Jacobian leg targets reached three claw contacts with no body collision before later instability; they are not enabled in Unity. Contact-only grip/wing ramp is being tested next.

## Landing iteration checkpoint
One Jacobian leg-target + contact-only claw grip + gradual wing ramp run passed floor touchdown: six claws, zero final body collisions, up cosine .91394, final speed .05701 cm/s, .1 s continuous stable hold. Evidence: unified-ik-claw-hold-evaluation.json. This is a single successful engineering trial, not a robust playable mode. A repeat of the extended experiment failed during takeoff/hover before descent. Upstream flight initialization randomizes wing-pattern phase; explicit environment seeds have now been added and cross-seed repeatability tests are running. Do not claim reliable landing or walking resume until those tests qualify. No limits were relaxed and no root force or physical state reset was added.

Unity scientific combat passed all 15 checks with all 138639 neurons and trained head stabilization enabled. Evidence: scientific-combat-head-full-brain-evaluation.json. Unity imported and displayed the new head toggle and active trained-head HUD state.
