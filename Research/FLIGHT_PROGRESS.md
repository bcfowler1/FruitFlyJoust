# Flight research progress, September 17, 2026

Latest integration supersedes historical exclusions below. WalkingLab now supports scientific-body gameplay combat and enemies on its simulation clock, physical rider unloading/restoration, optional artistic leg IK and hand-held weapons. Scientific combat passed 14 checks body-only and 15 with the full brain; enemies passed nine and clock/toggles eight. Exact observation caching retained all tested states and gave 1.1899× speedup. A headless full-FlyBody walking→wing-flight transfer passed two short hover conditions without pose/velocity reset or root lift forces; the lower hover condition failed. Wing-driven landing and biologically validated motor/sensory control remain outstanding. See CURRENT_STATUS.md for the actual scope and evidence.

NeuroMechFly follow-up: loaded walking now has qualified physical floor steering within drive 0.5–0.9, measured speed/yaw/contact feedback to the full brain, and same-body flat-surface grip/assisted-launch/landing/gait handover. Floor, wall and ceiling qualification and full-brain transport checks passed. This does not unify NeuroMechFly with FlyBody's wing-flight controller. External launch forces and synthetic neural feedback remain engineering interfaces. See NEUROMECHFLY.md.

Latest: calibrated a stable empirical translation surrogate on ten loaded flight references using window-averaged center-of-mass velocity, then tested held-out reference 48. RMS error is 0.018897 m/s, max error 0.041977 m/s. The real-time stream passed finite motion, pause, climb cue and reset checks (paced ratio 0.997). It has engineering yaw/rein mapping, fixed wing pose, no collisions and no live brain; it does not replace the scientific model or calibrate the combat prototype. See averaged-flight-calibration.json and fast-stream-evaluation.json. Initial simpler fits were rejected rather than deployed.

Implemented a separate averaged-wrench flight approach on the full articulated body. After four claws maintain settled contact for 20 ms, the controller removes flight support and leaves actual contact adhesion to hold the loaded fly. Launch releases claw control and applies averaged flight support. Floor/wall/ceiling finite approach-hold-launch tests passed; wall/ceiling stream checks also passed half-second hold and reset. Unity displayed wall approach/perch with four contacts and launch via rider Space. This is a millimeter-scale flat-surface engineering experiment, not wing-policy or neural landing control. See perch-transition-evaluation.json and perch-stream-evaluation.json. Neural calibration, combined scientific combat and broader landing environments remain unfinished.

Extended controller checks completed all 12 runs: ten full recorded references plus lateral root-velocity impulses of 0.02 and 0.05 meters/second on reference 48, applied after 50 ms. The longest available recorded reference completed 575 ms of simulated flight. See extended-flight-evaluation.json for tracking, height, clipping, timing, and termination results. This is a bounded reference test; sustained unrestricted flight and biological validation are not established.

Reference 2 required three action clips and reference 59 required five; the strict no-clipping rule remains unmet. All other tested runs had zero clips.

CombatLab passed 13 Unity Play-mode checks for runtime rider creation, refusal of airborne dismount, safe dismount from an actual perch, grounded foot controller, sword collision/cooldown, on-foot arrows, remount/visual restoration, target reset, mounted arrows, forward fly motion, and moving lance impact. The controlled test positions targets in the moving weapon path; it does not establish human controller feel or enemy combat. See combat-play-evaluation.json.

The optimized flight stream passed finite poses, motion, pause/resume, replay, reference completion, and rider-cue transport. Cue mapping to neural activity or flight actions remains explicitly disabled. See flight-stream-evaluation.json.

Identified 25 DNg02 candidate neurons (13 left, 12 right) in the public FlyWire 783 annotation dump, pinned to commit 8587524c1748ce5ef2080822a2fc890fc03bf597. All root IDs match the downloaded neural model. flight-populations.json records the source, dump SHA-256, anatomical identities, and published functional evidence. An anatomical name alone does not provide a quantitative rate-to-wing decoder, pitch controller, or sensory encoding.

The full CUDA neural model now supports optional per-neuron population spike counters during eager and captured-graph stepping. Sugar stimulation produced zero DNg02 spikes over the diagnostic window. Artificial direct excitation verified nonzero counted spikes in both groups; this is a counting diagnostic, not a biological sensory model. Neural flight decoding remains disabled. See flight-readout-evaluation.json.

A Tensor-returning tf.function wrapper around the unchanged checkpoint matched the original mean actions within tolerance. On a fixed observation, inference improved from about 998 to 310 microseconds per call (3.22 times faster). The longest recorded reference completed with the wrapper and no clipping, but full physics/control stepping still ran at about 0.060 times real time. The Unity backend uses this tested wrapper. It does not make interactive neural flight real time. See flight-performance-evaluation.json.

The user authorized completing the remaining integration and specified rider mass as one third of fly mass. The physical fly is 0.984621 mg; the rider is 0.328207 mg. Rigid spherical rider inertia is combined with the thorax using center-of-mass shifts and the parallel-axis theorem. Three loaded references completed with finite state and zero action clips (rider-load-evaluation.json). A two-second synthetic constant-speed reference also completed. This establishes bounded loaded-controller behavior, not biological payload accuracy.

An opt-in experimental closed loop now runs the actual 138,639-neuron model in an owned CUDA subprocess. Body speed/height/vertical speed/yaw feedback and rider cues enter a declared engineering controller that artificially excites identified DNg02 neurons. Actual counted left/right spikes produce small bounded wing-yaw residuals on top of the trained stabilizer. It completed reference 1 with matching neural/body clocks, actual spikes, nonzero residuals, and zero clips. Full neural state reset and reproducibility after reset passed. These are NOT biological sensory encoding or calibrated rate-to-wing mappings; pitch still comes from the trained controller, and most flight stabilization remains learned. See experimental-neural-flight-evaluation.json and neural-reset-evaluation.json.

The full articulated body now has a separate interactive six-claw contact experiment. It uses the upstream six contact-only adhesion actuators, without root welding. Loaded floor/wall/ceiling cases held six contacts for 500 ms; wall and ceiling controls without adhesion detached. Stream tests passed contact, pause, release, and reset. Gravity/poses are rotated consistently for world-upright display. This is a flat-surface contact experiment with fixed leg targets and uncalibrated adhesion gains, not a flight-to-perch landing controller. Use ResearchLab with the Six-Claw Wall/Ceiling backend menus. See adhesion-evaluation.json and adhesion-stream-evaluation.json.

Caching the immutable actuator mapping preserved final loaded state parity and improved full-reference wall time 1.79 times. Full loaded stepping was about 0.102 times real time in this test. A separate policy-rate sweep sustained two seconds at 5 kHz and 1 kHz, but 500 Hz and 200 Hz failed; those unstable rates are not used. Real-time whole-brain flight remains unresolved. See environment-performance-evaluation.json and multirate-flight-evaluation.json.

CombatEncounter adds swordsmen/archers, collision movement, visibility checks, ballistic opponent arrows, rider health, defeat, and reset to the gameplay prototype. Nine Unity Play-mode encounter checks passed. Combat remains separate from scientific body physics. Biological sensory encoding, quantitative flight calibration, combined flight/leg/perch control, and real-time performance remain outstanding.

Final integration checks: Unity displayed loaded experimental flight through reference completion with 138,639 neurons and zero clips. The wall contact lab held six claws, paused, released, detached, and reset in Editor Play mode. Queued spur/brake button edges now survive later neutral UDP packets and are consumed exactly once; the closed-loop regression passed. See unity-integration-evaluation.json and experimental-neural-flight-evaluation.json.
